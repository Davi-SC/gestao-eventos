<div class="col-md-5 mx-auto shadow p-3">

    <nav class="navbar bg-body-tertiary">
        <div class="container-fluid">
            <h3 class="navbar-brand">Editar Ingresso</h3>
        </div>
    </nav>

    <form method="post" action="/ingresso/salvar">
        <fieldset>

            <legend>Dados do Ingresso</legend>

            <div class="mb-3 col-3">
                <label for="id" class="form-label">CÓDIGO</label>
                <input type="number" value="{{ingresso.id}}" name="id" id="id" class="form-control" readonly>
            </div>

            <div class="row">

                <div class="mb-3 col-3">
                    <label for="tipo" class="form-label">TIPO</label>
                    <input type="text" value="{{ingresso.tipo}}" name="tipo" id="tipo" class="form-control" required>
                </div>
                <div class="col mb-3">
                    <label for="preco" class="form-label">PRECO</label>
                    <input value="{{ingresso.preco}}" type="number" id="preco" name="preco" class="form-control"
                        required>
                </div>
                <div class="col mb-3">
                    <label for="quantidade" class="form-label">QUANTIDADE</label>
                    <input value="{{ingresso.quantidade}}" type="number" id="quantidade" name="quantidade"
                        class="form-control" required>
                </div>
            </div>

            <button type="submit" class="btn btn-success">Salvar</button>

        </fieldset>
    </form>
</div>